<UserControl x:Class="Bimbrownik_Desktop.Views.Statistics.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Bimbrownik_Desktop.Controls">
    <DockPanel Background="White">

        <Border Background="#D0D0D0" Height="60" Padding="10" DockPanel.Dock="Top">
            <DockPanel LastChildFill="False">
                <TextBlock Text="{DynamicResource StatisticsTitle}"
                           FontSize="26" FontWeight="Bold"
                           VerticalAlignment="Center"
                           DockPanel.Dock="Left"/>
                <controls:LanguageToggle Width="32" Height="32"
                                         VerticalAlignment="Center"
                                         DockPanel.Dock="Right"/>
            </DockPanel>
        </Border>

        <ScrollViewer Margin="20">
            <StackPanel>
                <TextBlock Text="{DynamicResource TotalRecipes}" FontWeight="Bold"/>
                <TextBlock x:Name="TotalRecipesText" Margin="0,0,0,10"/>

                <TextBlock Text="{DynamicResource TotalCategories}" FontWeight="Bold"/>
                <TextBlock x:Name="TotalCategoriesText" Margin="0,0,0,10"/>

                <TextBlock Text="{DynamicResource RecipesViewedToday}" FontWeight="Bold"/>
                <TextBlock x:Name="ViewedTodayText" Margin="0,0,0,10"/>

                <TextBlock Text="{DynamicResource LastViewedRecipe}" FontWeight="Bold"/>
                <TextBlock x:Name="LastViewedText" Margin="0,0,0,10"/>

                <TextBlock Text="{DynamicResource TopViewedRecipes}" FontWeight="Bold"/>
                <StackPanel x:Name="TopViewedList" />
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>